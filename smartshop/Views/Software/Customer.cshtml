<!DOCTYPE html>
<html>
<head>
    <title>Customer</title>

    <!-- css -->
    @Styles.Render("~/Content/stylesheets")
    @Styles.Render("~/font-awesome/fonts")
</head>
<body>

    <!-- navbar menu -->
    <nav class="navbar navbar-inverse navbar-static-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a href="@Url.Action("Index","Home")" class="navbar-brand"><b>Videoke Rental System</b></a>
                @*<img src="../images/store.png" alt="Flower" class="navcode">*@
            </div>
            @Html.Partial("_LoginPartial")
            <div class="collapse navbar-collapse" id="myNavbar">
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="#"><i class="fa fa-envelope"></i></a></li>
                </ul>
            </div>
        </div>
    </nav>
    <br />
    <br />

    <div class="container">
        <div class="row">
            <div class="col-md-2">
                <div class="panel-group" id="accordion">
                    <div class="panel panel-default">
                        <div class="panel-heading panel-heading-custom">
                            <a href="@Url.Action("Index", "Software")" class=" panel-menu"><i class="fa fa-list"></i> Main menu</a>
                        </div>
                    </div>
                    <div class="panel panel-default">
                        <div class="panel-heading panel-heading-custom">
                            <a href="@Url.Action("Users", "Software")" class=" panel-menu"><i class="fa fa-user"></i> User</a>
                        </div>
                    </div>
                    <div class="panel panel-default">
                        <div class="panel-heading panel-heading-custom">
                            <a href="@Url.Action("Customer", "Software")" class="panel-active"><i class="fa fa-users"></i> Customer</a>
                        </div>
                    </div>
                    <div class="panel panel-default">
                        <div class="panel-heading panel-heading-custom">
                            <a href="@Url.Action("Product", "Software")" class="panel-menu"><i class="fa fa-microphone"></i> Videoke</a>
                        </div>
                    </div>
                    <div class="panel panel-default">
                        <div class="panel-heading panel-heading-custom">
                            <a href="@Url.Action("Calendar", "Software")" class="panel-menu"><i class="fa fa-calendar"></i> Calendar</a>
                        </div>
                    </div>
                    <div class="panel panel-default">
                        <div class="panel-heading panel-heading-custom">
                            <a href="@Url.Action("Message", "Software")" class="panel-menu"><i class="fa fa-globe"></i> Notification</a>
                        </div>
                    </div>
                    <div class="panel panel-default">
                        <div class="panel-heading panel-heading-custom">
                            <a href="@Url.Action("Reports", "Software")" class="panel-menu"><i class="fa fa-print"></i> Reports</a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-10">
                <h2>Customer</h2>
                <button type="button" class="btn btn-primary pull-right" data-toggle="modal" onclick="btnAddButton()"><span class="glyphicon glyphicon-plus"></span> Add</button>

                <br />
                <br />
                <table id="table_id" class="display">
                    <thead>
                        <tr class="tablecolor">
                            <th style="width: 60px"><center></center></th>
                            <th style="width: 60px"><center></center></th>
                            <th>Customer Number</th>
                            <th>Customer Name</th>
                            <th>Address</th>
                        </tr>
                    </thead>
                </table>

                <!-- Modal -->
                <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <form class="form-horizontal">
                                <div class="modal-header">
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                    <h4 class="modal-title" id="myModalLabel">User Info</h4>
                                </div>
                                <div class="modal-body">
                                    <input type="hidden" id="customer_Id" />
                                    <div class="form-group">
                                        <label class="col-sm-4 control-label">Customer Number</label>
                                        <div class="col-sm-8">
                                            <input type="text" id="customer_CustomerNumber" class="form-control requiredCustomer">
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="col-sm-4 control-label">Customer Name</label>
                                        <div class="col-sm-8">
                                            <input type="text" id="customer_CustomerName" class="form-control requiredCustomer">
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="col-sm-4 control-label">Street</label>
                                        <div class="col-sm-8">
                                            <input type="text" id="customer_CustomerStreetAddress" class="form-control requiredCustomer">
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="col-sm-4 control-label">Town</label>
                                        <div class="col-sm-8">
                                            <input type="text" id="customer_CustomerTownAddress" class="form-control requiredCustomer">
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="col-sm-4 control-label">City</label>
                                        <div class="col-sm-8">
                                            <input type="text" id="customer_CustomerCityAddress" class="form-control requiredCustomer">
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <label class="col-sm-4 control-label">Email</label>
                                        <div class="col-sm-8">
                                            <input type="email" id="customer_CustomerEmail" class="form-control requiredCustomer">
                                        </div>
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button class="btn btn-primary pull-left" onclick="btnCustomerDetail_onClick()">Customer Details</button>
                                    <button type="submit" class="btn btn-primary" onclick="btnSaveCustomer()">Save</button>
                                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>

                <!-- Modal -->
                <div class="modal fade" id="myModalDelete" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                <h4 class="modal-title" id="myModalLabel">Delete Customer</h4>
                            </div>
                            <div class="modal-body">
                                <input type="hidden" id="deleteCustomerId" />
                                Are you sure you want to delete this?
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-danger" onclick="btnDeleteCustomer()">Delete</button>
                                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
    <br />
    <footer class="footer">
        <div class="container">
            <div class="row">
                <nav>
                    <div class="col-lg-6">
                        <h4>Help</h4>
                        <ul style="list-style-type:none">
                            <li>
                                <a href="#" class="btnfooter">Contact</a>
                            </li>
                            <li>
                                <a href="#">Privacy Policy</a>
                            </li>
                            <li>
                                <a href="#">Terms of Service</a>
                            </li>
                        </ul>
                    </div>
                    <div class="col-lg-6">
                        <p>CONNECT WITH US</p>
                        <a href="#" class="btn btn-primary" data-placement="top" data-toggle="tooltip" title="Like on us Facebook"><i class="fa fa-facebook-official"></i></a>
                        <a href="#" class="btn btn-info" data-placement="top" data-toggle="tooltip" title="Follow on us Facebook"><i class="fa fa-twitter"></i></a>
                        <a href="#" class="btn btn-danger" data-placement="top" data-toggle="tooltip" title="Email on us Google"><i class="fa fa-google"></i></a>
                    </div>
                </nav>
            </div>
        </div>
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <hr />
                    <p>
                        <center><small>© copyright 2016-2017 Online Videoke Rental System. All Rights Reserved.</small></center>
                    </p>
                </div>
            </div>
        </div>
    </footer>

    @Scripts.Render("~/Scripts/js")
    <script type="text/javascript">
        // globla varialbles
        var customers;

        //get customers
        function getCustomer() {
            customers = new Array();
            $.ajax({
                url: '/api/customer/list',
                cache: false,
                type: 'GET',
                contentType: 'application/json; charset=utf-8',
                success: function (Results) {
                    if (Results.length > 0) {
                        for (i = 0; i < Results.length; i++) {
                            customers.push({
                                EditId: '<button class="btn btn-primary btn-sm btn-block" onclick="btnEditButton()"><span class="glyphicon glyphicon-edit"></span> Edit</button>',
                                DeleteId: '<button class="btn btn-danger btn-sm btn-block" onclick="btnDeleteButton()"><span class="glyphicon glyphicon-trash"></span> Delete</button>',
                                Id: Results[i]["Id"],
                                CustomerNumber: Results[i]["CustomerNumber"],
                                CustomerName: Results[i]["CustomerName"],
                                CompleteAddress: Results[i]["Street"] + ", " + Results[i]["Town"] + ", " + Results[i]["City"],
                                Street: Results[i]["Street"],
                                Town: Results[i]["Town"],
                                City: Results[i]["City"]
                            });
                        }
                    }
                    $('#table_id').dataTable({
                        aaData: customers,
                        aoColumns: [
                                { mData: 'EditId' },
                                { mData: 'DeleteId' },
                                { mData: 'CustomerNumber' },
                                { mData: 'CustomerName' },
                                { mData: 'CompleteAddress' },
                        ]
                    });
                }
            });
            return customers;
        }

        // Add Customer
        function btnSaveCustomer() {
            var customerObject = new Object();

            customerObject.CustomerNumber = document.getElementById('customer_CustomerNumber').value;
            customerObject.CustomerName = document.getElementById('customer_CustomerName').value;
            customerObject.Street = document.getElementById('customer_CustomerStreetAddress').value;
            customerObject.Town = document.getElementById('customer_CustomerTownAddress').value;
            customerObject.City = document.getElementById('customer_CustomerCityAddress').value;
            customerObject.Email = document.getElementById('customer_CustomerEmail').value;
            
            var customerData = JSON.stringify(customerObject);

            var isValid = true;
            $('.requiredCustomer').each(function () {
                if ($.trim($(this).val()) == '') {
                    isValid = false;
                    $(this).css({
                        "border": "1px solid red"
                    });
                }
                else {
                    $(this).css({
                        "border": "",
                    });
                }
            });
            if (isValid == false) {
                toastr.error("Invalid Entry");
            }
            else {
                var customerId = document.getElementById('customer_Id').value;
                if (customerId == 0) {
                    $.ajax({
                        type: "POST",
                        url: '/api/customer/add',
                        contentType: "application/json; charset=utf-8",
                        dataType: "json",
                        data: customerData,
                        statusCode: {
                            200: function () {
                                location.reload();
                            },
                            404: function () {
                                alert("Not Found");
                            },
                            500: function () {
                                alert("Bad Request");
                            }
                        }
                    });
                } else {
                    $.ajax({
                        type: "PUT",
                        url: '/api/updateCustomer/' + customerId,
                        contentType: "application/json; charset=utf-8",
                        dataType: "json",
                        data: customerData,
                        statusCode: {
                            200: function () {
                                location.reload();
                            },
                            404: function () {
                                alert("Not Found");
                            },
                            500: function () {
                                alert("Bad Request");
                            }
                        }
                    });
                }
            }
        }

        // edit
        function btnEditButton() {
            $('#myModal').modal({
                show: true,
                backdrop: 'static'
            });

            $('.requiredCustomer').each(function () {
                $(this).css({
                    "border": "",
                });
            });

            var table = $('#table_id').DataTable();

            $('#table_id tbody').on('click', 'tr', function () {
                document.getElementById('customer_Id').value = table.row(this).data().Id;
                document.getElementById('customer_CustomerNumber').value = table.row(this).data().CustomerNumber;
                document.getElementById('customer_CustomerName').value = table.row(this).data().CustomerName;
                document.getElementById('customer_CustomerStreetAddress').value = table.row(this).data().Street;
                document.getElementById('customer_CustomerTownAddress').value = table.row(this).data().Town;
                document.getElementById('customer_CustomerCityAddress').value = table.row(this).data().City;
            });
        }

        // delete
        function btnDeleteButton() {
            $('#myModalDelete').modal({
                show: true,
                backdrop: 'static'
            });
            var table = $('#table_id').DataTable();
            $('#table_id tbody').on('click', 'tr', function () {
                document.getElementById('deleteCustomerId').value = table.row(this).data().Id;
            });
        }
        function btnDeleteCustomer() {
            var customerId = document.getElementById('deleteCustomerId').value;

            $.ajax({
                type: "DELETE",
                url: '/api/deleteCustomer/' + customerId,
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                statusCode: {
                    200: function () {
                        location.reload();
                    },
                    404: function () {
                        alert("Not Found");
                    },
                    500: function () {
                        alert("Bad Request");
                    }
                }
            });
        }

        // Add
        function btnAddButton() {
            $('#myModal').modal({
                show: true,
                backdrop: 'static'
            });

            $('.requiredCustomer').each(function () {
                $(this).css({
                    "border": "",
                });
            });

            var customerLastNumber = new Array();
            $.ajax({
                url: '/api/customer/listLastCustomerNumber',
                cache: false,
                type: 'GET',
                contentType: 'application/json; charset=utf-8',
                success: function (Results) {
                    if (Results.length > 0) {
                        customerLastNumber.push({
                            EditId: '<button class="btn btn-primary btn-sm btn-block" onclick="btnEditButton()"><span class="glyphicon glyphicon-edit"></span> Edit</button>',
                            DeleteId: '<button class="btn btn-danger btn-sm btn-block" onclick="btnDeleteButton()"><span class="glyphicon glyphicon-trash"></span> Delete</button>',
                            Id: Results.Id,
                            CustomerNumber: Results.CustomerNumber,
                            CustomerName: Results.CustomerName,
                            Street: Results.Street,
                            Town: Results.Town,
                            City: Results.City
                        });


                    }

                    var CustomerNumberDefault = Results.CustomerNumber;

                    console.log("lahos sud");

                    document.getElementById('customer_Id').value = 0;
                    document.getElementById('customer_CustomerNumber').value = parseInt(CustomerNumberDefault) + 1;
                    document.getElementById('customer_CustomerName').value = "";
                    document.getElementById('customer_CustomerStreetAddress').value = "";
                    document.getElementById('customer_CustomerTownAddress').value = "";
                    document.getElementById('customer_CustomerCityAddress').value = "";


                }
            });

            console.log("lahos gawas");
        }

        function btnCustomerDetail_onClick() {
            var customer_id = document.getElementById('customer_Id').value;
            window.location = '/Software/CustomerDetails?CustomerId=' + customer_id;
        }
        // On Load Page
        $(document).ready(function () {
            document.getElementById('customer_CustomerNumber').disabled = true;

            getCustomer();
        });
    </script>
</body>
</html>
